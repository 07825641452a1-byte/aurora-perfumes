# 📝 دليل تعديل الأسعار والأسماء - شرح مبسط

## الطريقة الأولى: من خلال لوحة التحكم (الأسهل والأسرع) ✨

### الخطوات:

1. **انقر على زر "Code"** في لوحة التحكم (الزر الأزرق على اليمين)
   - ستظهر لك قائمة بالملفات

2. **ابحث عن الملف التالي:**
   ```
   client/src/data/products.ts
   ```

3. **افتح الملف** بالنقر عليه
   - ستظهر لك البيانات الحالية

---

## مثال عملي: تعديل منتج

### البيانات الحالية:
```javascript
{
  id: "1",
  name: "بلاك أوركيد",           // ← اسم المنتج
  type: "Eau de Parfum",
  price: "450 ر.س",              // ← السعر
  image: "/images/perfume-1.jpg",
  delay: 0
}
```

### لتعديل الاسم:
غيّر `"بلاك أوركيد"` إلى الاسم الجديد الذي تريده
```javascript
name: "عطري الملكي"  // ✅ الاسم الجديد
```

### لتعديل السعر:
غيّر `"450 ر.س"` إلى السعر الجديد
```javascript
price: "550 ر.س"  // ✅ السعر الجديد
```

---

## خطوات التعديل بالتفصيل:

### 1️⃣ افتح لوحة التحكم
- اضغط على زر **"Code"** في الزاوية العلوية

### 2️⃣ ابحث عن الملف
- اكتب في البحث: `products.ts`
- أو انقر على: `client → src → data → products.ts`

### 3️⃣ عدّل البيانات
- ابحث عن اسم المنتج الذي تريد تعديله
- غيّر الاسم أو السعر أو كليهما

### 4️⃣ احفظ التغييرات
- اضغط `Ctrl + S` (أو `Cmd + S` على Mac)
- أو ابحث عن زر "Save" في الواجهة

### 5️⃣ شاهد التغييرات
- اضغط على **"Preview"** لترى الموقع
- التغييرات ستظهر تلقائياً! 🎉

---

## أمثلة عملية:

### مثال 1: تعديل اسم ومنتج
**قبل:**
```javascript
{
  id: "2",
  name: "عود سيام",
  price: "850 ر.س",
  ...
}
```

**بعد:**
```javascript
{
  id: "2",
  name: "عود الملكي",
  price: "950 ر.س",
  ...
}
```

### مثال 2: إضافة منتج جديد
انسخ أي منتج موجود والصقه بعده، ثم عدّل البيانات:
```javascript
{
  id: "4",
  name: "الورد الفاخر",
  type: "Eau de Toilette",
  price: "380 ر.س",
  image: "/images/perfume-4.jpg",
  delay: 600
}
```

---

## ⚠️ نصائح مهمة:

✅ **افعل:**
- احفظ الملف بعد كل تعديل
- استخدم أسعار واضحة (مثل: "450 ر.س")
- تأكد من وجود الفواصل (الكوما) بين البيانات

❌ **لا تفعل:**
- لا تحذف الأقواس `{}`
- لا تحذف الفواصل `,`
- لا تغيير معرف المنتج `id` بدون سبب

---

## إذا حدث خطأ:

إذا رأيت رسالة خطأ بعد التعديل:
1. تحقق من وجود جميع الفواصل `,`
2. تأكد من وجود جميع الأقواس `{}`
3. تأكد من أن النصوص محاطة بـ `"` (علامات اقتباس)

---

**أسئلة؟ جرب الخطوات وأخبرني إذا واجهت أي مشكلة!** 💬
